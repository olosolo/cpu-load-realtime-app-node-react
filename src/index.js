import React from 'react';
import ReactDOM from 'react-dom';

import CpuLoadContainer from './containers/CpuLoadContainer';

const app = document.getElementById('app');

ReactDOM.render(
    <CpuLoadContainer />
, app);